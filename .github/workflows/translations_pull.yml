name: Pull translations

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        ruby-version: [2.7]

    steps:
      - name: "Run action"
        run: |
          curl -o- https://raw.githubusercontent.com/transifex/cli/master/install.sh | bash
          echo ${{ secrets.TX_TOKEN }}
          tx pull --token ${{ secrets.TX_TOKEN }} --hostname "https://rest.api.transifex.com"
